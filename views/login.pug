extends layout

append content
  div(class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom")
    h1(class="h2") Insira suas informações de entrada
  div(class="d-flex flex-column col-12 mt-3")
    div(class="mb-3")
      label(class="visually-hidden" for="input_authorization") Autorização
      div(class="input-group")
        div(class="input-group-text") #[span(class="material-icons" style="font-size: 2rem;") person_pin]
        input(type="text" class="form-control" id="input_authorization" aria-describedby="authorizationHelp" placeholder="Código exclusivo de acesso")
      small(id="authorizationHelp" class="form-text text-muted") Caso tenha perdido o seu código fale com o administrador do sistema.
    div(class="mb-3")
      label(class="visually-hidden" for="input_password") Password
      div(class="input-group")
        div(class="input-group-text") #[span(class="material-icons" style="font-size: 2rem;") lock]
        input(type="password" class="form-control" id="input_password" aria-describedby="passwordHelp" placeholder="Sua senha...")
        div(class="input-group-text visibility-eye") #[span(class="material-icons" style="font-size: 2rem;") visibility_off]
      small(id="passwordHelp" class="form-text text-muted") Nunca mostre sua senha a ninguém e nem passe por e-mail.
    button(type="button" class="btn btn-outline-primary col-12 p-2 mt-2" onclick="window.app.loginAuth()") #[span(class="material-icons") login]
  script(src="/javascripts/login.min.js")